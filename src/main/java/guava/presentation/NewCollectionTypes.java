package guava.presentation;

import com.google.common.collect.HashMultimap;
import com.google.common.collect.ImmutableSet;
import com.google.common.collect.Multimap;

import java.util.Set;

/**
 * There are a couple of interensting new collections introduced in Guava:
 * - MultiSet
 * - Bimap
 * - Table
 * - RangeSet
 * - RangeMap
 * - MultiMap
 * - ClassToInstanceMap
 *
 * I'll only exemplify the last 2 because those where the ones that caught my eye.
 * For the other ones refer to Guava's documentation
 */
public class NewCollectionTypes {

    MultiMapSection examplesOnMultiMaps;

    ClassToInstanceMapSection examplesOnClassToInstanceMap;

}
